<%# simple_form_for(@user_ingredient) do |f| %>
  <%# Dropdown for liquors %>
  <%# f.input :ingredient_id, :label => "Liquor", :collection => @liquors, :selected => @liquors.first, input_html: { multiple: true } %>
  <%# f.input :ingredient_id, :label => "Liqueur", :collection => @liqueurs, :selected => @liqueurs.first, input_html: { multiple: true } %>
  <%# f.input :ingredient_id, :label => "Syrup", :collection => @syrups, :selected => @syrups.first, input_html: { multiple: true } %>
  <%# f.input :ingredient_id, :label => "Juice", :collection => @juices, :selected => @juices.first, input_html: { multiple: true } %>
  <%# f.submit 'Go'%>
<%# end %>

<%= simple_form_for(@user_ingredient) do |f| %>
  <%# Dropdown for liquors %>
  <%= f.input :ingredient_id, :label => "Liquor", :collection => @liquors, input_html: { multiple: true } %>
  <%= f.input :ingredient_id, :label => "Liqueur", :collection => @liqueurs, input_html: { multiple: true } %>
  <%= f.input :ingredient_id, :label => "Syrup", :collection => @syrups, input_html: { multiple: true } %>
  <%= f.input :ingredient_id, :label => "Juice", :collection => @juices, input_html: { multiple: true } %>
  <%= f.submit 'Go'%>
<% end %>
